#include<stdio.h>
#include<dtdlib.h>
#include<math.h>

int main()
{
	int n, *v;
	
	printf("Digite a dimensao do vetor: ");
	scanf("%d", &n);
	
	v=(float*)malloc(n*sizeof(float));
	
	for(i=0, i<n; i++)
	{
		printf("\nDigite o %d")
	}
}
